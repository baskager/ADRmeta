export default class Portrait {
    constructor(azureFaceId, cho, type, title, description, beginTimestamp, imageUrl) {
        this.azureFaceId = azureFaceId,
        this.cho = cho,
        this.type = type,
        this.title = title,
        this.description = description,
        this.beginTimestamp = beginTimestamp,
        this.imageUrl = imageUrl
    }

}
